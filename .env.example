# PostgreSQL Database Configuration
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to .env (remove .example)
# 2. Replace YOUR_PASSWORD_HERE with your actual PostgreSQL password
# 3. Save the file
# 
# The .env file is in .gitignore, so your password won't be committed to Git.
# This .env.example file is safe to commit (it doesn't contain real passwords).
#
# DOCKER USERS:
# If using Docker (docker-compose.yml), the default password is "postgres"
# Make sure POSTGRES_PASSWORD in .env matches POSTGRES_PASSWORD in docker-compose.yml

# PostgreSQL Connection Settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YOUR_PASSWORD_HERE

# For Docker setup, use:
# POSTGRES_PASSWORD=postgres
